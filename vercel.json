{
  "buildCommand": "npx next build",
  "outputDirectory": "out",
  "installCommand": "npm install",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "out" }
    }
  ]
}
